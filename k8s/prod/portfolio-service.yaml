apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-service
  namespace: prod
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: portfolio-service
  template:
    metadata:
      labels:
        app: portfolio-service
    spec:
      containers:
        - name: portfolio-service
          image: tradeforgeacr001.azurecr.io/tradeforge/portfolio-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
  namespace: prod
spec:
  type: ClusterIP
  selector:
    app: portfolio-service
  ports:
    - port: 8000
      targetPort: 8000
