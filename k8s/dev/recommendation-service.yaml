apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-service
  namespace: dev
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: recommendation-service
  template:
    metadata:
      labels:
        app: recommendation-service
    spec:
      containers:
        - name: recommendation-service
          image: tradeforgeacr001.azurecr.io/tradeforge/recommendation-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: recommendation-service
  ports:
    - port: 8000
      targetPort: 8000
