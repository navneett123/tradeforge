apiVersion: apps/v1
kind: Deployment
metadata:
  name: market-service
  namespace: dev
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: market-service
  template:
    metadata:
      labels:
        app: market-service
    spec:
      containers:
        - name: market-service
          image: tradeforgeacr001.azurecr.io/tradeforge/market-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: market-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: market-service
  ports:
    - port: 8000
      targetPort: 8000
